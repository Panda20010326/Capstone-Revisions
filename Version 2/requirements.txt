streamlit>=1.35
pandas>=2.0
numpy>=1.26
joblib>=1.3
scikit-learn>=1.4
xgboost>=2.0
requests>=2.31
folium>=0.16
streamlit-folium>=0.21
plotly>=5.20

# Only needed once you drop in the trained ProfileEncoder artifacts
# (artifacts/profile_encoder/*.keras). The app runs fine without it using
# the built-in fallback -- install this when you're ready to switch over.
# tensorflow>=2.16
